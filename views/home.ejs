<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=1200, height=2000, initial-scale=1.0" />
  <title>Home</title>
  <style>
    :root {
      --text: #fff;
      --orange: #d88734;
      /* พื้นปุ่ม */
      --orange-2: #f0a24e;
      /* ไฮไลต์ปุ่ม */
      --orange-3: #b56b22;
      /* เงาปุ่ม */
    }

    html,
    body {
      height: 100%;
      margin: 0;
      background: #0b0c0e;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: system-ui, -apple-system, "Noto Sans Thai", "Prompt", sans-serif;
      color: var(--text);
    }

    /* กรอบจอมือถือ/โปสเตอร์ */
    .stage {
      position: relative;
      width: 1200px;
      height: 2000px;
      overflow: hidden;
      border-radius: 18px;
      background:
        linear-gradient(180deg, rgba(0, 0, 0, .15), rgba(0, 0, 0, .65) 55%, rgba(0, 0, 0, .85)),
        url('/img/firstpage/BGHomepage.png') center/cover no-repeat;
      box-shadow: 0 40px 100px rgba(0, 0, 0, .7), inset 0 0 0 1px rgba(255, 255, 255, .06);
    }

    /* มุมขวาบน: เปลี่ยนภาษา + ธงเล็ก */
    .lang {
      position: absolute;
      top: 72px;
      right: 72px;
      text-align: right;
    }

    .lang h3 {
      margin: 0 4px 10px 0;
      font-size: 22px;
      font-weight: 600;
      text-shadow: 0 2px 10px rgba(0, 0, 0, .55);
    }

    .flags {
      display: flex;
      gap: 12px;
      justify-content: flex-end;
    }

    .flag-btn {
      border: 0;
      background: transparent;
      padding: 0;
      border-radius: 8px;
      box-shadow: 0 0 12px rgba(252, 248, 236, .45), 0 0 28px rgba(247, 216, 122, .18);
      transition: transform .12s ease, box-shadow .18s ease;
    }

    .flag-btn img {
      display: block;
      width: 88px;
      height: 58px;
      border-radius: 6px;
      border: 1px solid rgba(255, 255, 255, .55);
    }

    .flag-btn:hover {
      transform: scale(1.05);
    }

    /* โลโก้กลางจอ */
    .logo-wrap {
      position: absolute;
      top: 460px;
      left: 0;
      right: 0;
      display: flex;
      justify-content: center;
      pointer-events: none;
    }

    .logo {
      width: 720px;
      height: 720px;
      object-fit: contain;
      border-radius: 50%;
      filter: drop-shadow(0 10px 18px rgba(0, 0, 0, .55));
    }

    /* ข้อความ "สั่งอาหารได้ที่นี่" */
    .tagline {
      position: absolute;
      top: 1320px;
      left: 0;
      right: 0;
      text-align: center;
      font-size: 48px;
      font-weight: 800;
      letter-spacing: .2px;
      text-shadow: 0 3px 12px rgba(0, 0, 0, .65);
    }

    /* ปุ่มสองอันด้านล่าง */
    .actions {
      position: absolute;
      bottom: 180px;
      left: 0;
      right: 0;
      display: flex;
      gap: 80px;
      justify-content: center;
    }

    .action {
      width: 300px;
      height: 300px;
      border-radius: 28%;
      text-decoration: none;
      color: var(--text);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      gap: 0;
      background:
        radial-gradient(180px 120px at 50% 22%, rgba(255, 255, 255, .18), rgba(255, 255, 255, 0) 70%),
        linear-gradient(180deg, var(--orange-2), var(--orange));
      border: 1px solid rgba(255, 255, 255, .08);
      box-shadow:
        0 20px 40px rgba(0, 0, 0, .35),
        0 0 40px rgba(208, 135, 52, .35);
      position: relative;
      overflow: hidden;
      transition: transform .12s ease, box-shadow .18s ease, filter .18s ease;
    }

    .action:hover {
      transform: translateY(-2px);
      box-shadow:
        0 26px 48px rgba(0, 0, 0, .45),
        0 0 52px rgba(208, 135, 52, .45);
      filter: saturate(1.05);
    }

    .action:active {
      transform: translateY(0);
    }

    .icon {
      width: 190px;
      height: 180px; 
      filter: drop-shadow(0 2px 6px rgba(0, 0, 0, .35));
    }

    .action span {
      font-size: 40px;
      font-weight: 800;
      text-shadow: 0 2px 8px rgba(0, 0, 0, .55);
    }

    .action::before {
      content: "";
      position: absolute;
      inset: 0;
      background-image: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" width="200" height="200"><filter id="n"><feTurbulence type="fractalNoise" baseFrequency="0.9" numOctaves="3" stitchTiles="stitch"/></filter><rect width="100%" height="100%" filter="url(%23n)"/></svg>');
      mix-blend-mode: multiply;
      opacity: 100%;
      pointer-events: none;
    }

    .action--sharp-tr { border-radius: 100px 10px 100px 100px; }
    .action--sharp-bl { border-radius: 100px 80px 100px 10px; }
  </style>
</head>

<body>
  <main class="stage">
    <!-- มุมขวาบน -->
    <div class="lang">
      <div class="flags">
        <a href="/thai" class="flag-btn" aria-label="ภาษาไทย">
          <img
            src="https://upload.wikimedia.org/wikipedia/commons/8/85/Flag_of_Thailand_%28proposed_shade_of_colours%29.png"
            alt="TH">
        </a>
        <a href="/eng" class="flag-btn" aria-label="English">
          <img
            src="https://upload.wikimedia.org/wikipedia/commons/thumb/8/83/Flag_of_the_United_Kingdom_%283-5%29.svg/512px-Flag_of_the_United_Kingdom_%283-5%29.svg.png"
            alt="EN">
        </a>
      </div>
    </div>

    <!-- โลโก้กลาง -->
    <div class="logo-wrap">
      <img class="logo" src="/img/logo.png" alt="Urban Street">
    </div>

    <!-- ข้อความ -->
    <div class="tagline">สั่งอาหารได้ที่นี่</div>

    <!-- ปุ่มเลือก -->
    <div class="actions">
      <a class="action action--sharp-bl" href="/main" aria-label="ทานที่ร้าน">
        <img src="/img/firstpage/diein.png" alt="dine-in" class="icon">
        <span>ทานที่ร้าน</span>
      </a>

      <a class="action action--sharp-tr" href="/home" aria-label="ทานที่บ้าน">
        <img src="/img/firstpage/home.png" alt="home" class="icon">
        <span>ทานที่บ้าน</span>
      </a>  
    </div>

    <div aria-hidden="true"></div>
  </main>
</body>

</html>