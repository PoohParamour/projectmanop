<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=1200, height=2000, initial-scale=1.0" />
    <title>Menu</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --text: #fff;
            --ink: #e5e7eb;
            --brown: #7b5237;
            --brown-2: #6a452d;
            --chip-gray: #e7e0d6;
            --chip-text: #2a2018;
            --orange: #d88734;
            --orange-2: #f0a24e;
            --glass: rgba(255, 255, 255, .06);
            --header-h: 210px;
            --footer-h: 230px;
            --radius-lg: 18px;
            --radius-xl: 25px;
            --card-bg: #ffffff;
            --card-text: #111111;
            --font-ui: "Kanit", system-ui, -apple-system, "Noto Sans Thai", "Prompt", sans-serif;
        }

        html,
        body {
            height: 100%;
            margin: 0;
            background: #0b0c0e;
            font-family: var(--font-ui);
            color: var(--text);
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .stage {
            position: relative;
            width: min(1200px, 100vw);
            height: min(2000px, 100vh);
            overflow: visible;
            border-radius: var(--radius-lg);
            background: linear-gradient(180deg, rgba(0, 0, 0, .15), rgba(0, 0, 0, .65) 55%, rgba(0, 0, 0, .85)),
                url('/img/firstpage/BGHomepage.png') center/cover no-repeat;
            box-shadow: 0 40px 100px rgba(0, 0, 0, .7), inset 0 0 0 1px rgba(255, 255, 255, .06);
        }

        .scroll-area {
            position: relative;
            height: 100%;
            overflow: visible;
        }

        .container {
            height: 100%;
            overflow-y: auto;
            -webkit-overflow-scrolling: touch;
            overscroll-behavior: contain;
            scrollbar-width: none;
            box-sizing: border-box;
            padding: 0 0 140px 0;
        }

        .container::-webkit-scrollbar {
            display: none;
        }

        /* NAVIGATION BAR */
        .bottom-nav {
            position: absolute;
            left: 0;
            right: 0;
            bottom: 0;
            height: 88px;
            background: #4a2f1f;
            color: #fff;
            display: grid;
            grid-template-columns: 1fr auto 1fr;
            align-items: center;
            padding: 0 24px;
            z-index: 40;
            box-shadow: 0 -2px 0 rgba(0, 0, 0, .25);
        }

        .bottom-nav .nav-action {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 14px;
            text-decoration: none;
            color: inherit;
            font-weight: 800;
            text-transform: uppercase;
            font-size: 40px;
            height: 100%;
        }

        .bottom-nav .nav-action svg {
            width: 32px;
            height: 32px;
            display: block;
        }

        .bottom-nav .nav-divider {
            width: 1px;
            height: 38px;
            background: rgba(255, 255, 255, .35);
            border-radius: 1px;
        }

        /* ==============================
   🎯 Responsive สำหรับ iPad Gen 7–10
   ============================== */
        @media (max-width: 1024px) {

            html,
            body {
                overflow: hidden;
                /* ❗️ล็อกไม่ให้ body ขยับ */
                background: #000;
            }

            .stage {
                width: 1200px;
                /* ✅ ล็อกขนาดกรอบ stage */
                height: 2000px;
                margin: 0 auto;
                transform: scale(0.85);
                /* ✅ ย่อทั้ง stage ให้พอดี iPad */
                transform-origin: top center;
                border-radius: 18px;
                box-shadow: none;
            }

            /* ✅ ให้เลื่อนเฉพาะเนื้อหาภายใน container */
            .scroll-area {
                height: 100%;
                overflow: hidden;
            }

            .container {
                height: 100%;
                overflow-y: auto;
                -webkit-overflow-scrolling: touch;
                padding-bottom: 180px;
                /* เว้นพื้นที่ footer */
            }

            /* ปุ่ม nav ด้านล่าง */
            .bottom-nav {
                height: 80px;
                font-size: 24px;
            }

            .bottom-nav .nav-action {
                font-size: 26px;
                gap: 10px;
            }

            .bottom-nav .nav-action svg {
                width: 28px;
                height: 28px;
            }
        }

        /* ==============================
   🎯 ปรับสำหรับแนวนอน (Landscape)
   ============================== */
        @media (max-height: 850px) and (orientation: landscape) {
            .stage {
                transform: scale(0.75);
                /* ย่อมากขึ้น */
                transform-origin: top center;
            }

            .bottom-nav {
                height: 70px;
            }

            .bottom-nav .nav-action {
                font-size: 22px;
            }
        }
    </style>
</head>

<body>
    <main class="stage">
        <section class="scroll-area">
            <div class="container">
                <%- include("menu") %>
            </div>
        </section>

        <!-- bottom navigation -->
        <nav class="bottom-nav" role="navigation" aria-label="Primary">
            <a class="nav-action nav-home" href="/">
                <svg viewBox="0 0 24 24" aria-hidden="true">
                    <path d="M3 10.5L12 3l9 7.5" fill="none" stroke="currentColor" stroke-width="2"
                        stroke-linecap="round" stroke-linejoin="round" />
                    <path d="M5 10v10h5v-6h4v6h5V10" fill="none" stroke="currentColor" stroke-width="2"
                        stroke-linecap="round" stroke-linejoin="round" />
                </svg>
                <span>
                    <%= lang==="th" ? "หน้าแรก" : "HOME" %>
                </span>
            </a>

            <span class="nav-divider" aria-hidden="true"></span>

            <a class="nav-action nav-cart" href="/cart">
                <svg viewBox="0 0 24 24" aria-hidden="true">
                    <path d="M6 6h15l-1.5 8.5H8.5L7 3H3" fill="none" stroke="currentColor" stroke-width="2"
                        stroke-linecap="round" stroke-linejoin="round" />
                    <circle cx="10" cy="20" r="1.6" fill="currentColor" />
                    <circle cx="18" cy="20" r="1.6" fill="currentColor" />
                </svg>
                <span>
                    <%= lang==="th" ? "ตะกร้า" : "CART" %>
                </span>
            </a>
        </nav>
    </main>
</body>

</html>