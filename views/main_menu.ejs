<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=1200, height=2000, initial-scale=1.0" />
    <title>
        <%= lang==="th" ? "เมนูอาหาร" : "Main Menu" %>
    </title>

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Kanit:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    <style>
        :root {
            --text: #fff;
            --ink: #e5e7eb;
            --brown: #4a2f1f;
            --brown-2: #6a452d;
            --orange: #d88734;
            --orange-2: #f0a24e;
            --font-ui: "Kanit", system-ui, -apple-system, "Noto Sans Thai", "Prompt", sans-serif;
            --divider: rgba(255, 255, 255, .08);
        }

        html,
        body {
            height: 100%;
            width: 100%;
            margin: 0;
            background: #0b0c0e;
            color: var(--text);
            font-family: var(--font-ui);
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* =====================
    🎯 STAGE
    ===================== */
        .stage {
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 1200px;
            height: 2000px;
            overflow: hidden;
            background:
                linear-gradient(180deg, rgba(0, 0, 0, .15),
                    rgba(0, 0, 0, .65) 55%, rgba(0, 0, 0, .85)),
                url('/img/firstpage/BGHomepage.png') center/cover no-repeat;
            box-shadow: 0 40px 100px rgba(0, 0, 0, .7),
                inset 0 0 0 1px rgba(255, 255, 255, .06);
        }

        /* =====================
    🎯 SCROLL AREA (ส่วนที่เลื่อน)
    ===================== */
        .scroll-area {
            position: absolute;
            top: 0;
            /* ✅ ไม่มี banner แล้วเริ่มจากบนสุดเลย */
            bottom: 88px;
            left: 0;
            right: 0;
            overflow-y: auto;
            -webkit-overflow-scrolling: touch;
            overscroll-behavior: contain;
            background: transparent;
        }

        .container {
            padding: 0 0 160px 0;
            box-sizing: border-box;
        }

        .scroll-area::-webkit-scrollbar {
            display: none;
        }

        /* =====================
    🎯 MENU STYLING (คงของเดิม)
    ===================== */
        .menu-topbar {
            position: sticky;
            top: 0;
            z-index: 20;
            background: #fff;
            height: 72px;
            display: flex;
            align-items: center;
            padding: 6px 30px;
            border-bottom: 2px solid #e5e7eb;
        }

        .chip-viewport {
            flex: 1 1 auto;
            overflow-x: auto;
            -webkit-overflow-scrolling: touch;
            scroll-behavior: smooth;
            scrollbar-width: none;
        }

        .chip-track {
            display: flex;
            align-items: center;
            gap: 28px;
            padding: 0 8px;
        }

        .chip {
            flex: 0 0 auto;
            color: #6b7280;
            font-size: 32px;
            font-weight: 800;
            padding: 0 16px;
            background: transparent;
            border: none;
            white-space: nowrap;
            cursor: pointer;
            transition: color .18s ease;
        }

        .chip.is-active {
            color: #10B34A;
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 40px;
            padding: 24px;
        }

        .menu-card {
            background: #FFFFFF80;
            border-radius: 30px;
            box-shadow: 0 12px 24px rgba(0, 0, 0, .35);
            color: #000;
            overflow: hidden;
        }

        .menu-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            aspect-ratio: 4 / 3;
            display: block;
            border-top-left-radius: 30px;
            border-top-right-radius: 30px;
        }

        .menu-title {
            text-align: center;
            font-weight: 600;
            font-size: 42px;
            margin: 10px 0;
        }

        .price {
            font-weight: 500;
            font-size: 40px;
        }

        /* =====================
    🎯 BOTTOM NAV (ติดล่าง)
    ===================== */
        .bottom-nav {
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            height: 88px;
            background: var(--brown);
            color: #fff;
            display: grid;
            grid-template-columns: 1fr auto 1fr;
            align-items: center;
            padding: 0 24px;
            box-shadow: 0 -2px 0 rgba(0, 0, 0, .25);
            z-index: 40;
        }

        .bottom-nav .nav-action {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 14px;
            text-decoration: none;
            color: inherit;
            font-weight: 800;
            text-transform: uppercase;
            font-size: 36px;
            height: 100%;
        }

        .bottom-nav .nav-action svg {
            width: 32px;
            height: 32px;
            display: block;
        }

        .bottom-nav .nav-divider {
            width: 1px;
            height: 38px;
            background: rgba(255, 255, 255, .35);
        }

        /* =====================
    📱 FIX SCROLL FOR MOBILE / TABLET
    ===================== */
        @media (max-width: 1024px) {

            html,
            body {
                position: fixed;
                inset: 0;
                margin: 0;
                padding: 0;
                height: 100%;
                width: 100%;
                overflow: hidden;
                touch-action: none;
                background: #000;
            }

            .stage {
                position: fixed;
                inset: 0;
                width: 100vw;
                height: 100vh;
                border-radius: 0;
                box-shadow: none;
                transform: none;
            }

            .scroll-area {
                position: absolute;
                top: 0;
                bottom: 88px;
                left: 0;
                right: 0;
                overflow-y: auto;
                -webkit-overflow-scrolling: touch;
                touch-action: pan-y;
                overscroll-behavior: contain;
            }

            .bottom-nav {
                position: fixed;
                bottom: 0;
                left: 0;
                right: 0;
                height: 88px;
            }
        }

        /* ===============================
📱 iPad Safari Full View Fix
=============================== */
        @supports (-webkit-touch-callout: none) {

            html,
            body {
                height: -webkit-fill-available;
            }

            .stage {
                height: -webkit-fill-available;
            }
        }

        /* ===============================
🎯 MEDIA QUERY สำหรับ iPad ทุกรุ่น (แนวตั้ง/แนวนอน)
=============================== */
        @media only screen and (min-device-width: 768px) and (max-device-width: 1024px) and (-webkit-min-device-pixel-ratio: 1) {

            html,
            body {
                height: -webkit-fill-available;
                overflow: hidden;
                background: #000;
            }

            .stage {
                position: fixed;
                inset: 0;
                width: 100vw;
                height: -webkit-fill-available;
                border-radius: 0;
                box-shadow: none;
                transform: none;
            }

            /* ✅ scroll ได้เฉพาะเนื้อหา */
            .scroll-area {
                position: absolute;
                top: env(safe-area-inset-top, 0px);
                bottom: calc(88px + env(safe-area-inset-bottom, 0px));
                left: 0;
                right: 0;
                overflow-y: auto;
                -webkit-overflow-scrolling: touch;
                touch-action: pan-y;
                overscroll-behavior: contain;
            }

            /* ✅ แก้ให้ bottom-nav เห็นตลอด */
            .bottom-nav {
                position: fixed;
                bottom: env(safe-area-inset-bottom, 0px);
                left: 0;
                right: 0;
                height: 88px;
                background: var(--brown);
                z-index: 9999;
            }
        }

        /* ===============================
📱 iPad Safari: Lock top chip-bar & bottom-nav ให้คงอยู่ตลอด
=============================== */
        @media only screen and (min-device-width: 768px) and (max-device-width: 1024px) and (-webkit-min-device-pixel-ratio: 1) {

            html,
            body {
                height: -webkit-fill-available;
                overflow: hidden;
                background: #000;
            }

            .stage {
                position: fixed;
                inset: 0;
                width: 100vw;
                height: -webkit-fill-available;
                border-radius: 0;
                box-shadow: none;
                transform: none;
            }

            /* ✅ scroll เฉพาะ content ระหว่าง chip-bar กับ nav-bar */
            .scroll-area {
                position: absolute;
                top: 72px;
                /* ความสูง chip-bar */
                bottom: calc(88px + env(safe-area-inset-bottom, 0px));
                left: 0;
                right: 0;
                overflow-y: auto;
                -webkit-overflow-scrolling: touch;
                overscroll-behavior: contain;
                touch-action: pan-y;
            }

            /* ✅ ล็อก chip bar ด้านบนไว้ตลอด */
            .menu-topbar {
                position: fixed !important;
                top: env(safe-area-inset-top, 0px);
                left: 0;
                right: 0;
                z-index: 9998;
                background: #fff;
                border-bottom: 2px solid #e5e7eb;
            }

            /* ✅ ล็อก bottom-nav ด้านล่างไว้ตลอด */
            .bottom-nav {
                position: fixed !important;
                bottom: env(safe-area-inset-bottom, 0px);
                left: 0;
                right: 0;
                height: 88px;
                z-index: 9999;
                background: var(--brown);
                box-shadow: 0 -4px 20px rgba(0, 0, 0, .5);
            }
        }
    </style>
</head>

<body>
    <main class="stage">
        <section class="scroll-area">
            <div class="container">
                <%- include("menu") %>
            </div>
        </section>

        <nav class="bottom-nav" role="navigation">
            <a class="nav-action" href="/">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                    stroke-linejoin="round">
                    <path d="M3 10.5L12 3l9 7.5"></path>
                    <path d="M5 10v10h5v-6h4v6h5V10"></path>
                </svg>
                <span>
                    <%= lang==="th" ? "หน้าแรก" : "Home" %>
                </span>
            </a>

            <span class="nav-divider"></span>

            <a class="nav-action" href="/cart">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"
                    stroke-linejoin="round">
                    <path d="M6 6h15l-1.5 8.5H8.5L7 3H3"></path>
                    <circle cx="10" cy="20" r="1.6"></circle>
                    <circle cx="18" cy="20" r="1.6"></circle>
                </svg>
                <span>
                    <%= lang==="th" ? "ตะกร้า" : "Cart" %>
                </span>
            </a>
        </nav>
    </main>
</body>

</html>